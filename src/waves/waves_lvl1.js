import k from "../kaplayCtx";
import { fighter } from "../entities/fighter";

export function makeWaves_lvl1() {
    let wave1Pos = [k.vec2(500, 0), k.vec2(600, 0), k.vec2(700, 0), k.vec2(800, 0), k.vec2(900, 0), k.vec2(1000, 0), k.vec2(1100, 0)];
}